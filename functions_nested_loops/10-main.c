#include "main.h"
#include <stdio.h>

int main(void)
{
    int result = add(10, 5);  /* Appel de la fonction add avec 10 et 5 */
    printf("The result of 10 + 5 is: %d\n", result);  /* Affichage du résultat */
    return 0;
}
